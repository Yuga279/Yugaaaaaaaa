# Architecture Notes

> PLACEHOLDER — run `/learn-codebase` (or `claude-engineer learn`) to replace this file
> with real notes about this repository. Other commands load this file as context.

When generated, this file describes:

- **Stack**: languages, frameworks, key libraries and their versions
- **Layout**: how the code is organized and what each top-level directory owns
- **Entry points**: how the application starts and how a request/job/event flows through the layers
- **Data**: databases, schemas/ORM, migrations, caching
- **Configuration**: env vars, config files, secrets handling, environments
- **Testing**: frameworks, how to run tests, coverage expectations
- **Build & CI**: build commands, lint commands, pipeline overview
- **External services**: APIs, queues, third-party integrations this system depends on
